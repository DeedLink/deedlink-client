// import { propertyContract } from "./contract";

// export const assignPoA = async (
//   tokenId: number,
//   agent: string,
//   right: string,
//   allowed: boolean,
//   start: number,
//   end: number
// ) => {
//   const tx = await propertyContract.methods
//     .setPoA(tokenId, agent, right, allowed, start, end)
//     .send({ from: window.ethereum.selectedAddress });

//   return { message: "Power of Attorney assigned successfully", tx };
// };
